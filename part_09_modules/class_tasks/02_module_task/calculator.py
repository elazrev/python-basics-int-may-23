def plus(a, b):
    return f"{a} + {b} = {a + b}"

def minus(a, b):
    return f"{a} - {b} = {a - b}"

def multi(a, b):
    return f"{a} + {b} = {a + b}"

def divid(a, b):
    return f"{a} + {b} = {a + b}"
